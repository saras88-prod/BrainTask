version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo "Install AWS CLI and kubectl"
      - pip install awscli
      - curl -LO "https://s3.us-west-2.amazonaws.com/amazon-eks/1.29.0/2024-07-18/bin/linux/amd64/kubectl"
      - chmod +x kubectl
      - mv kubectl /usr/local/bin/
      - echo "kubectl version:"

  build:
    commands:
      - echo "Build phase (if needed) – adjust as appropriate"
      # e.g., build Docker image, test, etc.
      - ls -R .

  post_build:
    commands:
      - echo "Deploying to EKS"
      - aws eks update-kubeconfig --name your-eks-cluster-name --region your-region
      - kubectl apply -f deployment.yaml
      - kubectl apply -f service.yaml
      - echo "Deployment complete."

artifacts:
  files:
    - deployment.yaml
    - service.yaml
    - # add any other files you want to include
